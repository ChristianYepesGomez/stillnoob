Deploy StillNoob frontend to Cloudflare Pages production.

Run: `npm -w @stillnoob/web run deploy`

The API on Render auto-deploys from git push, but the frontend does NOT — always run this after pushing frontend changes.