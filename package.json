{
  "name": "stillnoob",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "db:push": "npm -w @stillnoob/api run db:push",
    "db:generate": "npm -w @stillnoob/api run db:generate",
    "db:migrate": "npm -w @stillnoob/api run db:migrate"
  },
  "devDependencies": {
    "turbo": "^2.4.0"
  }
}
